# Required — OpenRouter API key for LLM access
OPENROUTER_API_KEY=sk-or-...

# Required — Matrix access tokens for each bot account
MATRIX_PLANNER_TOKEN=mct_...
MATRIX_BUILDER_TOKEN=mct_...

# Optional — GitHub PAT for agent git operations
GITHUB_TOKEN=ghp_...

# Optional — Tailscale auth key for SSH access into the container
# Create a reusable+ephemeral key at https://login.tailscale.com/admin/settings/keys
TAILSCALE_AUTHKEY=tskey-auth-...
TAILSCALE_HOSTNAME=nullclaw-modal
