# cerberus tool credentials — copy to .env and fill in real values
# .env is gitignored; .env.example is safe to commit

# Pushover notifications (used by the pushover tool)
# Register at https://pushover.net to obtain your token and user key
PUSHOVER_TOKEN=your_app_token_here
PUSHOVER_USER_KEY=your_user_key_here
