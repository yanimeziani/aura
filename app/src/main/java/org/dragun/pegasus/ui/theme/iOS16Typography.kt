package org.dragun.pegasus.ui.theme

import androidx.compose.material3.Typography

/**
 * Temporary stable typography preset for beta builds.
 * Keeps iOS16 symbol name used by LiquidGlassTheme while avoiding custom font resource failures.
 */
val iOS16Typography = Typography()
