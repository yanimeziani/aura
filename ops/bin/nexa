#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
NEXA_ROOT="${NEXA_ROOT:-$(cd "$SCRIPT_DIR/../.." && pwd)}"
export NEXA_ROOT
exec python3 "$NEXA_ROOT/nexa.py" "$@"
