# Meziani AI · Aura — R&D Status Log
**Date:** 2026-03-11
**Author:** Yani Meziani (Chairman / Product / Delivery)
**Classification:** Internal R&D Record

---

## Executive Summary

Aura is a sovereign, multi-agent operating system built for autonomous AI-driven wealth generation, B2B SaaS delivery, and infrastructure ownership. As of today the stack spans **Zig edge runtime, Python quant/agent layer, Next.js SaaS frontend, and a VPS-anchored sovereign infrastructure**, all orchestrated via a unified `aura` CLI. The build pipeline has been green for **772 consecutive overnight runs**. The first paying SaaS client (Dragun) is in production pilot.

---

## I. Core Architecture

```
Meziani AI (meziani.ai / meziani.org)
├── Aura OS              — monorepo sovereign stack  /home/yani/Aura
├── Dragun.app           — AI Debt Recovery SaaS     LIVE (production pilot)
├── Automation Stack     — night ops, n8n, proposals
└── Cybersec + AI Branch — security & ethics gating
```

### Language split
| Layer | Language | Components |
|---|---|---|
| Edge / systems | Zig 0.15.2 | aura-edge, aura-flow, aura-lynx, aura-tailscale, tui, ziggy-compiler, aura-taxes |
| Agency / quant | Python 3.12 | ai_agency_wealth (16+ modules), vault, gateway |
| SaaS frontend | Next.js / TypeScript | dragun-app, aura-landing-next |
| Infra / proxy | Caddy | gateway, sovereign-stack |

---

## II. Subsystem Status

### 1. Aura Edge (`aura-edge/`) — Zig runtime
- HTTP proxy, SNI routing, rate limiting, egress control, TLS cert management
- Source: `cert.zig`, `config.zig`, `egress.zig`, `http.zig`, `limit.zig`, `protection.zig`, `proxy.zig`, `sni.zig`
- New: `deploy-vps.sh`, `src/config.zig`, `src/egress.zig`, `src/protection.zig` (untracked → ready to commit)
- Binary: `zig-out/bin/aura_edge` — up to date

### 2. Ziggy Compiler (`ziggy-compiler/`) — Zig build tooling
- Lint pipeline with JSONL report output (`out/lint/report.jsonl`)
- Binary: `zig-out/bin/ziggyc` — up to date
- Cache refreshed; lint report updated

### 3. Quant Trading System (`ai_agency_wealth/`) — Python
- **8,061+ lines** across `quant_signals.py`, `quant_risk.py`, `quant_backtest.py`, `algo_trader.py`, `crypto_controller.py`
- Strategy: multi-signal composite (technical 40% / microstructure 25% / sentiment 20% / on-chain 15%)
- Regime detection: ADX + Hurst → TRENDING / RANGING / VOLATILE
- Risk: Half-Kelly sizing, 1.5% SL / 3% TP, 2:1 RR, trailing stops, 5% daily drawdown circuit breaker
- Exchange: Coinbase Advanced Trade via CCXT
- Daemons: `trader_daemon.py`, `trader_ctl.py`, `aura-trader.service`
- Watchdog state (as of 19:10 UTC): **HEALTHY — 0 issues**
- Paystubs generated: 2026-03-09, 03-10, 03-11 (daily agent payroll running)

### 4. Dragun App (`dragun-app/`) — AI Debt Recovery SaaS
- Stack: Supabase (Postgres + pgvector + Auth), Stripe Connect, Groq AI (RAG), Arcjet, Sentry, Vercel
- Auth: `dragun.app/login` → Google OAuth → unified portal
- Portals: Merchant Dashboard (`/dashboard`), Debtor Portal (`/pay/:id`, `/chat/:id`)
- Production pilot: **Venice Gym Charlesbourg** (Mounir — first paying client)
- Competitive target: replace Debtor Raptor
- CI/CD: GitHub → Vercel (preview on PR, prod on main merge)
- Full doc suite: PRD, PROD_READINESS_REPORT, SECURITY, DEPLOYMENT, DESIGN_SYSTEM, PILOT_GAPS

### 5. Aura Landing (`aura-landing-next/`) — Next.js
- High-end private company landing page (committed 2026-03-08)
- Vercel brand deployment ready (committed 2026-03-09)
- Files modified: `globals.css`, `layout.tsx`, `page.module.css`, `page.tsx`

### 6. Aura Taxes (`aura-taxes/`) — Zig
- QuickBooks clone scaffolded with Zig 0.15.2 lock (committed 2026-03-07)
- Source in `src/`, Zig build system active

### 7. Vault (`vault/`) — Secret management + owner OS
- `aura-vault.json` — encrypted API keys
- `aura-2fa.enc` — 2FA backup
- `life_dashboard.py`, `log_radio.py`, `log_radio_deck.py` — personal ops layer
- `log2notebooklm.py` — AI knowledge pipeline
- Browser sync, Chrome backup, KDE Connect gate

### 8. Sovereign Stack (`sovereign-stack/`) — VPS orchestration
- Caddy TLS termination, Fedora VPS, Tailscale mesh (`fedora.tailafcdba.ts.net`)
- Deploy scripts: `deploy-next.sh`, `deploy-remote.sh`

### 9. Automation Stack
- **Night Ops v2.1:** `~/.gemini/antigravity/scratch/night_ops.sh` — full stack launcher (⚠ manual, no systemd yet)
- **The Eye:** RSS → Ollama qwen3:8b → auto-proposals → SMTP (`yani@meziani.ai`) every 3h
- **The Sniper:** B2B outreach engine (`sniper-node-zig`)
- **n8n:** Cashflow Fastlane workflow — webhook → lead → auto-reply → +2h bump → +24h close
- **MAMS:** Simulation engine + ValidatorAgent (HVI, Safe Mode, deterministic traces)

### 10. Cybersec + AI Branch
- Permanent review authority on security-critical and high-impact AI deployments
- Covers: app security, infra security, secrets, AI safety/misuse, data governance, red-team, release gating, incident response
- Ethics Committee: required approval for high-impact AI/security launches
- Charter: `docs/CYBERSEC_AI_BRANCH.md`

### 11. MCP Layer (`aura-mcp/`, `mcp/`)
- MCP server integration — active in build system
- Integrations: Gmail, Google Calendar, Stripe, Supabase (all connected)

---

## III. Build Health

| Metric | Value |
|---|---|
| Consecutive overnight builds green | **772** |
| Last green build | 2026-03-10T14:52:46 |
| Watchdog status | **HEALTHY** |
| Quant system issues | **0** |
| Branch | HEAD |

---

## IV. Open / Untracked Work (unstaged changes)

The following represent active in-flight work — **not yet committed:**

| Area | Files |
|---|---|
| aura-edge | `deploy-vps.sh`, `src/config.zig`, `src/egress.zig`, `src/protection.zig` |
| ziggy-compiler | cache + lint report updates |
| ai_agency_wealth | `latest_newsletter.md`, `watchdog_state.json` |
| aura-landing-next | CSS, layout, page source (modified) |
| vault | `life_dashboard.py`, `log2notebooklm.py`, `mission_control_gui.py`, `vault_manager.py`, `forge_checkpoint.txt`, `forge_failed.txt` |
| docs | `HIERARCHY.md`, `ONBOARDING.md`, `roster.md`, `CYBERSEC_AI_BRANCH.md` (new) |
| bin | `aura`, `forge-run.sh` |
| dragun-app | submodule pointer drift |
| root | `CONTRIBUTING.md`, `LICENSE`, `SECURITY.md` (new) |

---

## V. Product Roadmap Snapshot

### Live
- [x] Dragun.app AI debt recovery SaaS — production pilot (Mounir)
- [x] Aura quant trading system — paper → live mode ready
- [x] Aura edge runtime (Zig) — deployed
- [x] Aura landing page — Vercel

### Imminent
- [ ] Commit untracked aura-edge protection/egress/config modules
- [ ] Commit vault ops updates
- [ ] Dragun: close pilot gaps (`PILOT_GAPS.md`), scale merchant onboarding
- [ ] Night Ops: add systemd timer (remove manual dependency)
- [ ] Aura Taxes: complete QuickBooks clone feature set

### Medium-term
- [ ] Aura Flow — `Coming Soon` in portal
- [ ] MAMS production deployment
- [ ] Algeria SMB dept activation (`algeria_smb_dept.py`)
- [ ] Wealthsimple dept activation (`wealthsimple_dept.py`)
- [ ] Canada / MENA segment split on meziani.ai

---

## VI. Key Personnel / AI Roster

| Role | Entity |
|---|---|
| Chairman / Product / Delivery | Yani Meziani |
| Builder Agent | Full-stack delivery, endpoints, UI |
| Data Agent | Collections intel, scoring, segmentation |
| QA/Ops Agent | Reliability, audit, rollback, export |
| GTM Agent | Displacement strategy, investor narrative |

---

## VII. Infrastructure Topology

```
Fedora Linux VPS (primary production server)
├── Tailscale mesh    fedora.tailafcdba.ts.net
├── Caddy TLS         config/Caddyfile
├── n8n               port 5678 (Tailscale-gated)
├── aura-edge         edge runtime / proxy
└── Python daemons    trader, autopilot, pay server, agency

GitHub (private)
└── Vercel CI/CD      dragun-app, aura-landing-next
                      preview on PR · prod on main
```

---

*End of R&D status log — 2026-03-11*
